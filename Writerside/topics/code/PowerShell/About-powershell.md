# PowerShell

<include from="project-library.md" element-id="about"/>

在 Windows 中 PowerShell 脚本是默认携带的，用于丰富、扩展甚至逐步替换 cmd 的工具，而在 Windows 运维中，PowerShell
脚本能完成各种特定的、流程化的、自动化的任务。

随系统按照的版本是 PowerShell5 版本，其`.ps1` 编码是 `UTF-8 BOM`

使用 PowerShell7 则不需要 BOM，其编码是标准的 UTF-8。