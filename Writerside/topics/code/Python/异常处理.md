# 异常处理

```Python
try:
    """可能会产生错误的代码"""
except ValueError:
    """值错误异常执行的代码"""
except ZeroDivisionError:
    """除0错误异常执行的代码"""
except:
    """除上面两种错误之外其他错误异常执行的代码"""
else:
    """没有错误时运行的代码"""
finally:
    """无论出不出错都执行"""

```